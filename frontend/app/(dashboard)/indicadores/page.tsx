import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function IndicadoresPage() {
  return (
    <PlaceholderPage
      title="Indicadores"
      description="Gráficos e indicadores agregados (conformidade, distribuição por área/tipo, tendência)."
      nextStep="Tela em construção — depende dos endpoints GET /api/dashboard/* (próxima etapa do roadmap)."
    />
  );
}
