import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function PontosPage() {
  return (
    <PlaceholderPage
      title="Pontos de Inspeção"
      description="Listagem e filtro dos pontos de inspeção de SPDA cadastrados."
      nextStep="Tela em construção — depende do endpoint GET /api/inspection-points (próxima etapa do roadmap)."
    />
  );
}
