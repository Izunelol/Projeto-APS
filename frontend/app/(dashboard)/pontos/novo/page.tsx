import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function NovoPontoPage() {
  return (
    <PlaceholderPage
      title="Novo Ponto de Inspeção"
      description="Cadastro de um novo ponto de SPDA com geração automática de código."
      nextStep="Formulário em construção — depende do endpoint POST /api/inspection-points (próxima etapa do roadmap)."
    />
  );
}
