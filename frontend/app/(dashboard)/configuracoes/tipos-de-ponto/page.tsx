import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function TiposDePontoPage() {
  return (
    <PlaceholderPage
      title="Tipos de Ponto"
      description="Cadastro das siglas de tipo de ponto de inspeção (CI, DESC, HC, etc)."
      nextStep="Tela em construção — o backend já expõe GET/POST /api/point-types via lib/api/pointTypes.ts."
    />
  );
}
